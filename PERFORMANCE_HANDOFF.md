# プレイヤー名表示 軽量化 引き継ぎメモ

## 対象

`/home/index/dev/suimag-playernames-improved`

主な対象:

- `playernames_cl.lua`
- `playernames_sv.lua`

## 現在の処理

`playernames_cl.lua` の `updatePlayerNames()` は `SetTimeout(0, updatePlayerNames)` により、ほぼ毎フレーム実行される。

表示が有効なプレイヤーに対して、主に以下を行っている。

- PedとPed座標の取得
- 距離計算
- 視線判定
- ゲーマータグの生成・再生成判定
- ゲーマータグの表示・非表示
- ボイスアイコン状態の更新
- Alpha設定の適用
- 色設定の適用
- 各種タグ設定の再適用

表示が無効なプレイヤーについてはPed座標・距離・視線判定までは行われないが、以下は毎フレーム実行される。

- ローカルプレイヤー座標の取得
- 全アクティブプレイヤーの走査
- 表示対象判定
- 既存タグの削除判定

タグの実生成は、タグが存在しない、Pedが変更された、またはゲーム側でタグが削除された場合に限られる。ただし、その条件確認自体は毎フレーム行われる。

名前変更はイベントを契機に行われる。サーバー側では500msごとに全アクティブプレイヤーの名前をレンダリングし、前回値と異なる場合に更新イベントを送っている。

## 軽量化の優先順位

### 1. 表示OFF時の更新頻度を下げる

`showSelf` と `showOthers` が両方無効でも、クライアント更新処理は毎フレーム呼ばれている。

推奨:

- 両方OFFの場合は250〜500ms間隔
- タグが残っている場合は設定変更時に即座に削除
- `templateStr` が未設定の場合も毎フレームの再試行を避ける
- いずれかがONの場合は必要な更新周期へ戻す

低リスクで効果が期待できるが、OFF時は既に重いPed座標・LOS処理が省かれているため、効果量は`resmon`で確認する。

### 2. プレイヤー退出・スロット再利用時の状態を削除する

`mpGamerTags`と`mpGamerTagSettings`はクライアントプレイヤーindexをキーにしている。

現状は非アクティブになったindexの設定を明示的に削除していないため、同じindexを別のプレイヤーが再利用した場合に、古い設定が残る可能性がある。

250〜500ms間隔の保守処理で、非アクティブindexについて以下を削除する。

- ゲーマータグ
- `mpGamerTagSettings`
- LOSキャッシュ
- Visibility・Alpha・Colorなどの適用済み状態
- Pedに紐づくその他のキャッシュ

Ped変更やタグ再生成時にも、適用済み状態を初期化する。

### 3. 最終状態を計算し、同じ設定を再適用しない

以下は値が変わったときだけ実行する。

- `SetMpGamerTagVisibility`
- `SetMpGamerTagAlpha`
- `SetMpGamerTagColour`
- `SetMpGamerTagWantedLevel`
- `SetMpGamerTagHealthBarColour`

単純にNative呼び出し単位で前回値を保存するだけでは不十分。

現状は、既定値を設定した後にoverrideを適用している。同じコンポーネントにoverrideがある場合、毎フレーム次のような反転が発生する。

```text
既定値を設定 → override値を設定
```

推奨手順:

1. 距離・LOS・ボイス状態を取得
2. 既定値とoverrideを合成
3. コンポーネントごとの最終的なdesired stateを確定
4. 前回適用した最終値と比較
5. 変化したNativeだけを呼ぶ

キャッシュはプレイヤーindexだけでなく、現在のタグまたはPedと関連付ける。タグ再生成、Ped変更、プレイヤー退出時には必ず破棄する。

### 4. 視線判定を間引く

`HasEntityClearLosToEntity`は毎フレームではなく、100〜200ms間隔で実行する。

初期値として100〜150msを推奨する。キャッシュはPed単位で保持し、以下の場合に無効化する。

- Ped変更
- タグ再生成
- プレイヤー退出
- 長距離から表示範囲内へ入った直後

### 5. 処理を頻度別に分離する

| 処理 | 推奨頻度 |
|---|---:|
| 非アクティブプレイヤーの掃除 | 250〜500ms |
| タグ生成・Ped変更確認 | 250ms |
| 距離判定 | 50〜150ms |
| 視線判定 | 100〜150ms |
| ボイスアイコン | 50ms |
| 名前変更 | イベント発生時のみ |
| 色・Alpha変更 | 設定変更時、またはタグ再生成時のみ |

複数の独立ループに分ける場合は、削除済みタグへのアクセスや古いPedのキャッシュを避ける。単一のスケジューラ内でタイムスタンプにより各処理を間引く方式も候補とする。

### 6. 不要な全タグrenameを避ける

現在の`playernames:settingsUpdated`処理は、誰か1人の設定変更でも全タグに`rename = true`を設定する。

推奨:

- プレイヤー設定変更時は、該当server IDに対応するタグだけをrename
- テンプレート変更時のみ全タグをrename

### 7. 距離計算を軽くする

ベクトル長ではなく距離の二乗で比較する。

```lua
local dx = pedCoords.x - localCoords.x
local dy = pedCoords.y - localCoords.y
local dz = pedCoords.z - localCoords.z
local distanceSquared = dx * dx + dy * dy + dz * dz

if distanceSquared < 250.0 * 250.0 then
    -- 表示処理
end
```

効果は限定的だが、プレイヤー数が多い場合に有効。

あわせて、ループ前に以下を一度だけ取得して使い回す。

```lua
local localPlayer = PlayerId()
local localPed = PlayerPedId()
local localCoords = GetEntityCoords(localPed)
```

## サーバー側の追加候補

`playernames_sv.lua`の`detectUpdates()`は、表示設定に関係なく500msごとに全アクティブプレイヤーのテンプレートをレンダリングしている。

クライアント側の軽量化後、サーバーCPUが問題になる場合は以下を検討する。

- 名前やテンプレートに影響するイベントでdirty flagを立てる
- dirtyなプレイヤーだけ再レンダリングする
- 定期処理は整合性確認用として低頻度化する

外部リソースがテンプレートcontextを動的に拡張している可能性があるため、完全なイベント駆動化は互換性を確認してから行う。

## 推奨実装順

1. 表示OFF時の待機処理
2. 退出プレイヤーとスロット再利用時の状態削除
3. 最終desired stateの合成と差分適用
4. LOSを100〜150ms、ボイスを50msへ間引く
5. Ped・タグ生存確認を250msへ分離
6. 設定変更時のrename対象を該当プレイヤーだけに限定
7. 距離二乗比較とNative取得値の使い回し
8. 必要に応じてサーバー側の500ms処理を最適化

## 確認方法

FiveMの`resmon 1`を使い、変更前後で以下を比較する。

1. 名前表示OFF
2. 自分の名前だけ表示
3. 他プレイヤー表示ON
4. プレイヤー数が多い状態
5. ステータス表示あり
6. 遮蔽物の出入りを繰り返す状態
7. ボイスの開始・終了を繰り返す状態
8. 死亡・リスポーンまたはPed変更
9. プレイヤー退出後に同じクライアントindexが再利用される状態

確認する値:

- リソースのCPU使用時間
- FPS
- プレイヤー数増加時の負荷変化
- 表示切り替え時の遅延
- LOS変化から表示反映までの遅延
- ボイスアイコンの追従性
- リスポーン後のタグ再生成
- 退出済みプレイヤーの設定が別プレイヤーへ残らないこと

## 現在の状態

軽量化に関するコード変更はまだ行っていない。
